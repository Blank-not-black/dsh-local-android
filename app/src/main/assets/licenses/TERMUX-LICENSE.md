# Termux upstream license notice

This notice is included because the embedded Android runtime is assembled from
the Termux package ecosystem. It is an attribution for the Termux-derived
runtime contents, not a license notice for the DSH for Android application as a
whole.

The `termux/termux-app` repository is released under GPLv3 only license.

### Exceptions

  * Terminal Emulator for Android code is used which is released under Apache 2.0 license. Check `terminal-view` and `terminal-emulator` libraries.
  * Check `termux-shared/LICENSE.md` for `termux-shared` library related exceptions.

Source: https://github.com/termux/termux-app/blob/master/LICENSE.md
